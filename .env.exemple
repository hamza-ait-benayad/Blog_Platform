#--------------------------------------------------------------------
# Example Environment Configuration File
#
# This file provides default settings and placeholders that can 
# be used to configure your application. Uncomment and set 
# the values as needed for your environment.
#--------------------------------------------------------------------

#--------------------------------------------------------------------
# ENVIRONMENT
#--------------------------------------------------------------------
# The application environment. Options: development, production, testing.
# Use 'development' for local development and 'production' for live deployments.
CI_ENVIRONMENT=development

#--------------------------------------------------------------------
# APP
#--------------------------------------------------------------------
# The base URL of your application (e.g., http://localhost/ or https://example.com/).
# app.baseURL=

# Whether to force all traffic to use HTTPS. Set to true for secure environments.
# app.forceGlobalSecureRequests=false

# Enable or disable Content Security Policy (CSP) headers for added security.
# app.CSPEnabled=false

#--------------------------------------------------------------------
# DATABASE
#--------------------------------------------------------------------
# Database connection settings.

# The hostname or IP address of your database server (e.g., localhost, 127.0.0.1).
database.default.hostname=

# The name of your database.
database.default.database=

# The username for connecting to the database.
database.default.username=

# The password for the database user.
database.default.password=

# The database driver to use (e.g., MySQLi, Postgre, SQLite3).
database.default.DBDriver=

# Optional: Port number for the database (default for MySQL is 3306).
# database.default.port=3306

#--------------------------------------------------------------------
# ENCRYPTION
#--------------------------------------------------------------------
# The encryption key used for securing sensitive data. 
# Generate a strong key for production environments.
# encryption.key=

#--------------------------------------------------------------------
# SESSION
#--------------------------------------------------------------------
# Session handler settings.

# The session driver to use (e.g., CodeIgniter\Session\Handlers\FileHandler).
# session.driver=CodeIgniter\Session\Handlers\FileHandler

# The save path for session files. Leave null to use the default.
# session.savePath=null

#--------------------------------------------------------------------
# LOGGER
#--------------------------------------------------------------------
# Logger settings for application debugging and error tracking.

# Logging threshold levels: 
# 0 = Disables logging, 1 = Error messages, 2 = Debug, 3 = Info, 4 = All messages.
# logger.threshold=4

#--------------------------------------------------------------------
# EMAIL SETTINGS
#--------------------------------------------------------------------
# Email server credentials.

# The email server address (e.g., smtp.example.com).
email_serve=

# The password for the email server.
password_serve=
